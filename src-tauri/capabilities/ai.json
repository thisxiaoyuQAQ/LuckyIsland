{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ai",
  "description": "Capability for the AI palette window (M7): 拖动",
  "windows": ["ai-palette"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-start-dragging"
  ]
}
