{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the island window",
  "windows": ["island"],
  "permissions": ["core:default", "dialog:default", "opener:default", "updater:allow-check"]
}
