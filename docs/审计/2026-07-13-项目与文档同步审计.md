# 2026-07-13 项目与文档同步审计

> 状态：🚫 六路只读审计已完成；DOC-10A、FIX-10A-01/02 已完成，其余整改暂停排队，当前入口已切换到模块 11
> 审计日期：2026-07-13
> 执行入口：[vault/CURRENT.md](../../vault/CURRENT.md)
> 整改总控：[vault/10-审计整改.md](../../vault/10-审计整改.md)

## 1. 目的与结论

本次由 6 个子 Agent 分别核对核心文档、开发进度/Vault、React 前端、Tauri/Rust 后端、测试工程配置及整体架构。项目实际主架构——React 多窗口 UI、Tauri 系统能力、SQLite 持久化、command/event IPC——总体连贯，**没有推倒重写或更换框架的必要**。

主要问题是：

1. 早期规划、当前事实、宣传描述混用；
2. 少量已宣布完成的行为仍有缺口；
3. 测试与可复现构建基线落后于功能规模；
4. 若干生命周期和安全边界值得渐进治理，但多数尚不是已确认 Bug。

## 2. 范围、限制与事实优先级

已检查：`项目备忘录.md`、`README.md`、`docs/*.md`、`docs/superpowers/**`、`vault/*.md`、`src/**`、`src-tauri/**`、前后端依赖与测试配置。

限制：本轮不修改产品代码；静态审计不能替代 Windows 真机、安装包、外部 Provider 和 24 小时长跑验证。尝试执行 `pnpm test`、`pnpm exec tsc --noEmit`、`pnpm build` 时，被本机 pnpm 对过期 `https://registry.npm.taobao.org` 的供应链校验阻断，未进入项目代码；当前 Bash 的 Rust 工具链也不在 `PATH`。因此本轮不能声称项目测试通过或失败。

状态冲突按以下顺序裁决：

1. `vault/CURRENT.md` 当前执行快照；
2. `docs/开发进度.md` 顶部状态表和最新验收；
3. 对应模块/BUG Vault 的最新结论；
4. 当前代码与新鲜验证；
5. spec（设计基线）；
6. plan（历史执行脚本）。

历史 plan 的未勾选框不是当前 TODO；旧“下一 Session”文字不是当前命令。

## 3. 发现与处置映射

### 3.1 文档事实漂移

| 审计 ID | 结论与证据 | 置信度 | 处置 |
|---|---|---|---|
| AUD-DOC-01 | 配置真源是 SQLite `settings` KV，不是 TOML 热重载。旧描述见 `项目备忘录.md:13,18`、`docs/技术栈规划.md:271-282`；实现见 `src-tauri/src/storage/mod.rs:22-25,70-86`。 | 确定 | DOC-10A-01 |
| AUD-DOC-02 | 实际未使用 Zustand/TanStack Query；状态为 React 本地状态、外部 store 与 Rust 轮询。见 `package.json:13-42`、`src/App.tsx:71-91`。 | 确定 | DOC-10A-01；不引入旧规划依赖 |
| AUD-DOC-03 | 语音技术已由 Porcupine 改为 sherpa-onnx + cpal + SAPI。见 `src-tauri/src/voice/mod.rs:1-5`、`src-tauri/Cargo.toml:62-63`。 | 确定 | DOC-10A-01 |
| AUD-DOC-04 | `lucky-notify` 是本地 HTTP 客户端，不是命名管道，也无离线队列。见 `src-tauri/src/bin/lucky-notify.rs:60-80`、`src-tauri/src/notify/server.rs:49-55`。 | 确定 | DOC-10A-01 |
| AUD-DOC-05 | Codex 当前由后端天气/DDG 预搜索后注入证据，测试禁止依赖 `--search`。见 `src-tauri/src/ai/provider.rs:137-159,1055-1072`。 | 确定 | DOC-10A-01 |
| AUD-DOC-06 | README 的“流式问答”与三个 Provider 的整包结果实现不符。见 `README.md:28-31`、`src-tauri/src/ai/provider.rs:34-77,653-752`。 | 确定 | DOC-10A-01；先改宣传，不重写 Provider |
| AUD-DOC-07 | Specs/plans 仍写待实施或全未勾选，但开发进度与 Vault 已有完成证据。 | 确定 | DOC-10A-02；保留历史语境并增加最终状态摘要 |
| AUD-DOC-08 | 多屏恢复同时被描述为“不主动跳回”“OS 自动恢复”“已实现”。实际恢复分支不主动定位，见 `src-tauri/src/monitor.rs:427-464`。 | 确定存在契约冲突 | DOC-10A-03 + RUN-10C-01 |
| AUD-DOC-09 | 文档中的 Vite 6、tracing/anyhow/thiserror/keyring、若干 Tauri 插件和目录树不是当前依赖/结构。 | 确定 | DOC-10A-01 |
| AUD-DOC-10 | 时间组件、AI 取消、多屏运行时回退、双动作热键等已实现能力未被核心文档完整吸收。 | 确定 | DOC-10A-01 |
| AUD-DOC-11 | 测试数量、Vitest 版本、素材方案与历史验收快照缺少命令/日期/范围标注。 | 确定 | DOC-10A-02 + BASE-10B-01 |

### 3.2 确定性行为缺口

| 审计 ID | 失败场景与证据 | 处置 |
|---|---|---|
| AUD-FIX-01 | 应用持续运行跨午夜时，木鱼只在挂载时 rollover，心情无午夜/visibility 刷新。见 `WoodenFishWidget.tsx:51-92`、`MoodWidget.tsx:23,37-40`。 | FIX-10A-01：先写可控日期测试，再统一日期边界刷新 |
| AUD-FIX-02 | 木鱼卸载 cleanup 只清除防抖定时器，最后约 500ms 的计数可能未落盘。见 `WoodenFishWidget.tsx:64-66`。 | FIX-10A-02：显式 flush 并测试快速切页/卸载 |
| AUD-FIX-03 | 原审计基于旧 AI 方案中的 `island://switch-page` 设想；DOC-10A 后当前 `ai/router.rs` 与 `ai/prompt.rs` 只允许 `reply`/`add_todo`，并明确禁止 AI 操控岛窗口。全仓不存在可触发的 `switch_page` 成功路径。 | ✅ 2026-07-14 用户确认按过时审计前提关闭；不新增 AI 页面控制能力 |

### 3.3 工程基线

| 审计 ID | 结论 | 处置 |
|---|---|---|
| AUD-BASE-01 | `pnpm test` 只覆盖时间页纯逻辑；Vitest 仅匹配 `*.test.ts` 且为 node 环境。 | BASE-10B-01/02 |
| AUD-BASE-02 | 缺少统一 typecheck、前端/Rust test/check、fmt/lint、verify 入口。 | BASE-10B-01 |
| AUD-BASE-03 | Node/pnpm 未锁定，Rust/MSVC/DLL/registry 前置条件不完整；本机旧淘宝 registry 证书过期。 | BASE-10B-03 |
| AUD-BASE-04 | `tsconfig.node.json` 未覆盖 Vitest 配置，Vite/Vitest alias 重复；三入口构建虽对齐但缺 smoke test。 | BASE-10B-01/02 |
| AUD-BASE-05 | Rust 约 43 项测试未进入 README 验证说明，原生依赖与打包 DLL 约束主要靠注释。 | BASE-10B-01/03 + RUN-10C-04 |

### 3.4 渐进重构机会

| 审计 ID | 结论 | 优先级/处置 |
|---|---|---|
| AUD-REF-01 | 多处异步 Tauri `listen()` 存在卸载先于 Promise 完成的泄漏竞态；`AiPalette` 已有 disposed 模式可参考。 | P0，REF-10B-01 |
| AUD-REF-02 | 三入口主题初始化与监听重复。 | P1，REF-10B-02 |
| AUD-REF-03 | `App.tsx` 聚合页面注册、设置、窗口状态、事件、导航与动画。 | P1，REF-10B-03；只提取稳定边界 |
| AUD-REF-04 | SQLite 只有无版本内联建表，旧安装升级风险随 schema 增长。 | P1，REF-10B-04 |
| AUD-REF-05 | Release 主要依赖 `eprintln!`，缺持久、脱敏、轮转日志。 | P1，REF-10B-05 |
| AUD-REF-06 | IPC 泛型/断言无运行时校验；Weather 并发状态和 AiPalette 状态机耦合较高。 | P2，REF-10B-06；渐进迁移 |
| AUD-REF-07 | `ai/provider.rs` 职责过多。 | 高风险候选 RISK-10C-03；保持协议/取消语义后再拆 |

### 3.5 高风险候选（不是已确认 Bug）

| 审计 ID | 候选风险 | 处置门禁 |
|---|---|---|
| AUD-RISK-01 | PTY 自然退出可能不从 registry 回收，reader 线程生命周期难管理。 | RISK-10C-01：先建自然退出/kill/shutdown 测试与复现矩阵 |
| AUD-RISK-02 | 语音下载在 async command 内做阻塞 I/O，状态由多个原子/Mutex 分散表达。 | RISK-10C-02：先测试取消、失败回滚、并发启停；不改已验 ASR 参数 |
| AUD-RISK-03 | Provider 文件拆分可能触及进程树、stdin、联网和取消稳定契约。 | RISK-10C-03：独立设计；不重开旧 AI BUG |
| AUD-RISK-04 | AI 动作、副作用、取消和历史写入的提交/幂等语义不完整。 | RISK-10C-04：先由产品定义取消点与副作用语义 |
| AUD-RISK-05 | 全局 command 面较宽、CSP 为 null、通用 settings key 无强类型白名单。 | RISK-10C-05：先做窗口×command 权限矩阵和回归测试 |
| AUD-RISK-06 | `lib.rs` 聚合职责，窗口错误可能被吞掉；语音/终端存在 Windows 平台细节散落。 | RISK-10C-06：结构重构前先补错误与生命周期测试 |

### 3.6 产品范围待决

以下是“需求仍保留但实现仅部分完成”或契约冲突，不得在无用户决策时直接编码：

| 审计 ID | 项目 | 处置 |
|---|---|---|
| AUD-PROD-01 | 待办截止时间、优先级、编辑、排序、提醒 | PROD-10C-01：决定 0.2.x 补齐或降为后续版本 |
| AUD-PROD-02 | 专注模式、默认页/上次页、Tab/切页方案配置 | PROD-10C-02 |
| AUD-PROD-03 | 通知是否打断、通知声音、窗口尺寸配置 | PROD-10C-03 |
| AUD-PROD-04 | AI 历史 30 天保留、窄范围撤销、动作前确认 | PROD-10C-04；历史清理优先于通用 Undo |
| AUD-PROD-05 | 点击穿透究竟指 hidden 后不命中，还是透明区域选择性穿透 | PROD-10C-05；先明确语义 |

### 3.7 真机与安装态验证

| 审计 ID | 验证项 | 处置 |
|---|---|---|
| AUD-RUNTIME-01 | 副屏恢复是否可作为稳定应用保证 | RUN-10C-01 |
| AUD-RUNTIME-02 | `lucky-notify.exe` 是否入包/PATH，CLI 数据目录是否与 Tauri 一致 | RUN-10C-02 |
| AUD-RUNTIME-03 | 点击穿透在 compact/expanded 透明区域的真实行为 | RUN-10C-03 |
| AUD-RUNTIME-04 | Release 包体、冷启动、CPU、内存、DLL 完整性 | RUN-10C-04 |
| AUD-RUNTIME-05 | 24h 无泄漏长跑与终端/语音/轮询资源变化 | RUN-10C-05；复用 `vault/07-DIY打磨.md` 的既有 P2 项 |
| AUD-RUNTIME-06 | 语音模型下载、真实麦克风、SAPI、安装包 DLL | RUN-10C-06 |

## 4. 不建议实施的重构

- 不为匹配旧文档引入 Zustand、TanStack Query 或 TOML 双配置源。
- 不为目录美观迁移全部文件。
- 不在无锁竞争证据时将 rusqlite Mutex 改成连接池。
- 不强迫 Claude CLI、Codex CLI、Chat API 共用同一传输实现。
- 不为一个撤销动作建设通用 Undo Bus。
- 不为 README 宣传立即重写三个 Provider 的流式协议。
- 不进行无产品目标的跨平台重写。

## 5. 去重和误判处理

- `BUG-20260710-01`～`05` 与 `08a` 均保持关闭；本报告只引用其稳定契约，不创建同名待办。
- `docs/superpowers/plans/*.md` 是历史执行脚本，未勾选项不自动成为任务。
- “项目不存在 `vault/`”是子 Agent 误判，已剔除；当前 `vault/` 存在且是任务载体。
- “敏感 command 已可被任意窗口利用”等安全结论尚缺动态证明，保留为加固候选，不表述为已确认漏洞。
- 页面全面 keep-alive、IPC 全量 schema 化等建议已收窄，避免一次性过度设计。

## 6. 推荐顺序

1. 文档事实源收敛；
2. 三个确定性缺陷及失败测试；
3. 修复开发环境/统一验证入口；
4. Tauri listener 生命周期与 React 测试层；
5. SQLite migration、日志、主题与 App 轻量拆分；
6. 高风险候选先复现/设计，再决定是否派生任务；
7. 真机矩阵和文档收尾。

## 7. 覆盖与遗漏复核

> 复核结论：✅ 2026-07-13 完成“六路只读审计、七类发现/处置”的覆盖性与反向一致性复核。

- [x] 所有 `AUD-*` 均映射到 `10a/10b/10c` 的任务或明确“不做/历史覆盖”。
- [x] 每个叶子任务均补有来源、状态、风险、依赖/门禁和验收方式；10c 候选以复现/决策/真机门禁而非“完成代码”关闭。
- [x] 无旧 BUG 被标为待处理，无历史 plan 被当成当前执行单。
- [x] `vault/CURRENT.md` 只有一个首个动作：DOC-10A-01。
- [x] 高风险候选均为待复现/待决策，未授权直接编码。
- [x] 新增 Markdown 的相对链接与目标路径经只读复核合理。
- [x] 本轮未运行到项目测试的事实已如实记录。

### 复核中发现并补强

1. 为 `10a/10b/10c` 增加统一任务元数据，补足风险、依赖与验收门禁。
2. 明确口径为“六路审计、七类处置（文档、缺陷、基线、渐进重构、高风险、产品、真机）”。
3. 收敛 `docs/开发进度.md` 的多屏表述：主动保证断开回退；恢复自动跳回仅是当时 Windows OS 观察，不是应用保证。
4. 确认不存在孤儿或重复审计 ID；`AUD-REF-07` 正确升级为高风险 Provider 拆分候选。
5. 确认 `vault/` 实际存在，已剔除子 Agent 的相反误判。

### 仍留给正式执行阶段的检查

- 当前审计证据大量引用 `path:line`；后续代码修改后行号会漂移，执行时应同时按符号/语义搜索，不只依赖固定行号。
- 当前无 Git 仓库元数据，无法绑定审计到 commit；正式执行前记录工作区文件状态和环境版本。
- Markdown 链接已按路径层级复核，但本轮未引入额外 Markdown link checker；若工程基线新增该工具，再纳入统一 `verify`。
