# 开发进度

## 状态：⬜ 待开发 | 🚧 进行中 | ✅ 已完成 | 🚫 阻塞

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 01 | 基础脚手架 | ✅ | — | pnpm dev/build/tauri dev 全部通过 |
| 02 | 灵动岛外壳 | ✅ | 01 | 块1✅ 块2✅；块3 多屏选择/立即移动/启动恢复/缺失回退/运行时副屏断开回退均已完成自动化与真机验收 |
| 03 | 基础页面 | ✅ | 02 | 切换框架+时间+日历(农历)+待办(CRUD+SQLite) |
| 04 | 数据页面 | ✅ | 02,03 | 天气(uapis 多城市+IP定位)/股票(腾讯+搜索+K线+拖拽排序) |
| 05 | 终端集成 | ✅ | 02,03 | xterm+portable-pty 多 tab + 快捷命令 + 外部WT + tab拖拽/改名 |
| 06 | 通知系统 | ✅ | 02,03 | HTTP + lucky-notify CLI + 通知页 + SQLite 历史 + Windows toast + hook 文档 |
| 07 | DIY打磨 | ✅ | 03,04,05,06 | 设置面板✅ + 安装包✅(NSIS) + 动画打磨✅ + 性能优化✅(语音闲置320->110MB/CPU<1%/基线61MB/F8.5毛玻璃降级/重启托盘项) |
| 07a | 窗口外观与配置导入导出 | ✅ | 07 | 透明度+双轴偏移（DPI clamp/切屏/重启持久化）+ 安全配置导入导出（可迁移 settings 白名单+自选股+天气城市，密钥/token/缓存不外泄）；Rust check、monitor 10/10、pnpm build 与真机验收通过 |
| 08 | AI助手 | ✅ | 02 | M7–M9 核心链路、录音提示、Codex 联网、跨 Provider 请求取消及 Provider 状态矩阵均已完成自动化与真机验收 |
| BUG-20260710-01 | 修复：ASR 转写重复字 | ✅ | 08 | 单变量恢复 blank penalty=0.0 后无重复；真机认可，voice 测试 5/5 + cargo check 通过；纳入 2026-07-11 收尾提交 |
| BUG-20260710-02 | 修复：Provider 状态与实时问答 | ✅ | 08 | Provider request/persisted/providerUsed 一致性、双窗口同步、重启持久化和 Codex 实时联网均于 2026-07-11 真机验收通过 |
| BUG-20260710-03 | 修复：按需录音提示不显示 | ✅ | 08 | async command + spawn_blocking + RecordingGuard + boolean listener 已实现；2026-07-11 用户真机确认提示正常显示；纳入收尾提交 |
| BUG-20260710-04 | 修复：Codex 多行 Prompt 传输截断 | ✅ | 08 | stdin 完整传输与自动化已通过；2026-07-11 LuckyIsland/Codex CLI 真机问答验收通过，不再答非所问；纳入收尾提交 |
| BUG-20260710-05 | 修复：Codex 联网超时 | ✅ | 08 | A+ 后端天气/DDG 预搜索、证据门禁与低思考 Codex 已实现；2026-07-11 联网天气真机验收通过，未再触发 120 秒超时；纳入收尾提交 |
| 08a | AI 请求取消与单活生命周期 | ✅ | 08 | Claude/Codex CLI 进程树与 Chat API/tool 链路真取消、A→B 立即复用、late-result/清理隔离和历史保护均于 2026-07-11 真机验收通过 |
| 09 | 时间页可自定义组件 | ✅ | 03 | 流式画布（上下满行+左右窄列+中央时钟）+一言/历史/运势/木鱼/心情+时间组件/时间外观面板+Rust UAPI 缓存+time:data 不导出；Vitest 45 + tsc 通过，真机验收通过；2026-07-13 审计发现的跨午夜/卸载 flush 遗留转入模块 10 |
| 10 | 审计整改 | 🚧 | 01–09 | DOC-10A-01/02 已完成；下一项 DOC-10A-03 多屏与点击穿透产品契约确认。见 `vault/CURRENT.md`、`vault/10-审计整改.md` |

### 2026-07-13 DOC-10A-01 当前架构与能力基线完成
- 对照前后端 manifest、`App.tsx`、SQLite storage、notify/ai/voice/lib 与实际目录逐项核验后，更新 README、需求文档、技术栈、AI 方案及 vault/02、06、08。
- 当前式统一为 SQLite settings KV、React 本地状态/外部 store、Vite 7、CLI→HTTP 通知、三个 AI Provider 的差异化联网路径、AI 完整回复非流式、前端 pending 与真正取消、sherpa-onnx KWS/流式 ASR + cpal + SAPI、多屏断开主动回退和两个可配置动作热键。
- Porcupine、应用 TOML、命名管道/离线队列、`codex --search`、Zustand/TanStack Query、虚构目录/migration 仅在明确的历史未采用语境中保留；外部 `~/.codex/config.toml` 和流式 ASR 保留为合法例外。
- 本次仅修改文档/状态文件，不改产品源码，也不产生新的产品测试数字；DOC-10A 仍进行中，下一项为 DOC-10A-02。

### 2026-07-13 六路审计与模块 10 建档
- 通过 6 个只读子 Agent 交叉核对核心文档、开发进度/Vault、React、Tauri/Rust、测试工程配置和整体架构；结论为主架构健康，无需推倒重写。
- 审计证据已落盘至 `docs/审计/2026-07-13-项目与文档同步审计.md`；整改总控为 `vault/10-审计整改.md`，分为 10a 文档/确定性修复、10b 工程基线/渐进重构、10c 高风险候选/产品与真机验证。
- 当前唯一跨会话入口为 `vault/CURRENT.md`；历史 `docs/superpowers/plans/*.md` 的未勾选框不再视作 TODO，BUG-20260710-01～05 与 08a 保持关闭。
- 本次仅建档与同步权威说明，未修改产品代码。`pnpm test`、`pnpm exec tsc --noEmit`、`pnpm build` 被本机过期 `registry.npm.taobao.org` 证书校验阻断，未执行到项目代码；当前 Bash 的 Rust 工具链不在 PATH，故本次不声称测试通过或失败。
- 确定性缺口已登记：时间组件运行中跨午夜刷新、木鱼卸载前 flush、AI `switch_page` 链路；正式修复留待下一对话按 10a 执行。
- 落盘后覆盖/反向一致性复核已完成：六路审计归并为七类处置，全部 `AUD-*` 有任务映射；10a/10b/10c 已补状态、风险、依赖/门禁与验收；无旧 BUG 重开、无历史 plan 误转 TODO、CURRENT 仅有 DOC-10A-01 一个首项，高风险项均未授权直接编码。详见审计报告第 7 节。


- 流式时间画布：上下满行（一言/历史单行展开）+ 左右窄列 + 中央时钟，区域交换/排序/降级均符合设计。
- 新增组件：一言（UAPI+缓存+离线短句）、程序员历史上的今天（轮换+详情+缓存）、今日运势（每日换签/重抽/动画）、电子木鱼（SVG 图+mp3 音效+敲击放大动效+防抖持久化+疯狂星期四彩蛋）、今日心情（五档+连续天数）。
- 设置拆为「时间组件」「时间外观」两个独立面板：时钟位置/颜色/双色渐变/9 个主题预设实时生效；Switch 开关替换原生打钩框。
- Rust `time_api` 模块走共享 reqwest + 8s 超时 + SQLite 缓存 + stale 降级；可迁移设置白名单纳入 `time:` 但排除 `time:data:`，功德/心情/运势等运行数据不随配置导入导出。
- 自动化：Vitest 纯逻辑 45 passed（布局/外观/日期/运势/组件配置）；Rust 单测 7 passed（time_api 6 + portable 白名单 1）；tsc 通过；`git diff --check` 通过。真机验收通过。
- 许可证边界：未复制 DailyFortunePlugin / muyu 源码；文案、SVG 木鱼、疯狂星期四文案均为原创；木鱼图与音效改用用户提供的 public/WoodenFish.svg 与 sound_1.mp3。

### 2026-07-11 07a 窗口外观与配置导入导出验收
- 设置页新增「外观」：背景透明度 10%~100%（默认 70%，保持原视觉）、横向/纵向偏移实时预览、重置默认；偏移按目标显示器 DPI 换算展开态物理高度并 clamp，切屏/重启/副屏回退均保留。
- 配置导出/导入使用系统原生文件对话框；JSON version=1，包含可迁移 settings、自选股与天气城市；明确排除 `ai:chat_api_key`、`notify:http_token`、天气/股票缓存和机器位置等敏感/运行时数据。
- 导入在事务内覆盖可迁移配置和两张业务表，逐条广播 `settings://changed`，并用 `config://imported` 即时刷新当前股票/天气页；偏移导入后立即上屏。
- 自动化：独立 `CARGO_TARGET_DIR` 下 `cargo check --lib` 通过；`cargo test --lib monitor::tests` 10 passed / 0 failed；`pnpm build` 通过；`git diff --check` 通过。
- 真机：透明度、双轴偏移、极值防出屏、重置、持久化/切屏、导出 JSON 安全字段、导入覆盖与即时刷新全部通过。

- 设置页依次切换 Claude CLI、Codex CLI、自定义 Chat API，AI 面板标签均同步；三个探针均由当前 Provider 正常回答，无 `Provider 状态不一致` 或 `Provider 响应不一致`。
- AI 面板反向依次切换三个 Provider，设置页标签均同步，双窗口状态保持一致。
- 完全退出并重启 LuckyIsland 后，两处 UI 均恢复最后选择的 Provider，重启后的实际请求路由正确。
- 结论：request/persisted/`providerUsed` 与两处 UI 标签一致性通过，BUG-20260710-02 和 AI 助手模块 08 标记完成。

### 2026-07-11 08a 请求取消真机验收
- Claude CLI：运行中终止约 1 秒返回，本次请求 PID 随进程树退出；取消后立即发送 B 正常完成，无 A 晚返回、状态污染、历史污染或后端错误日志。
- Codex CLI：运行中终止约 1 秒返回，取消后立即发送 B 正常完成，无 A 晚返回或状态污染；补测确认本次新增 Codex PID 及子进程取消后消失，后端无错误日志。
- Chat API：在 DuckDuckGo 响应读取/工具阶段（`DDG 返回 202 Accepted，HTML 14570 字节`）终止约 1 秒返回；取消后没有继续工具或模型轮次，B 可立即发送并正常完成，A 未写历史且无晚返回。
- 结论：08a 的跨 Provider 真取消、单活槽位立即释放、requestId/messageId 隔离、旧请求清理保护、资源释放与副作用防护全部通过，模块标记完成；未调整任何 ASR/KWS/VAD 参数，也未重新处理已关闭 BUG。

### 2026-07-11 真机验收与 Session 收尾
- 本机 Codex 全局配置已恢复为可解析状态，`codex exec` 可正常启动。
- LuckyIsland 中使用 Codex CLI 提问“今天天兰州天气怎么样”等实时天气问题，已能基于联网证据回答，不再只返回自我介绍，也未再触发 120 秒超时；用户真机验收通过。
- 当时 BUG-03、BUG-04 和 BUG-05 已标记完成；BUG-02 的 Provider 状态矩阵与 08a 的取消竞态在该次收尾时仍待真机，现均已于本页上方记录的 2026-07-11 验收中通过。
- Hook 配置与通用文档已收敛：Claude Code 使用 `PreToolUse(ask_user_question)` / `Notification` / `Stop`，Codex 使用 `PermissionRequest` / `Stop`，Codex 不配置 `UserPromptSubmit`。
- 用户随后要求不再保留未提交工作；当时按功能拆分提交全部业务代码与文档。该段中的“下一 Session 从 08a 验收开始”仅是 2026-07-11 的历史时点说明，相关验收已于本页上方完成，不再具有当前执行权威。

### 2026-07-10 自动化验证记录
- `cargo test --manifest-path src-tauri/Cargo.toml codex_ -- --nocapture`：8 passed，0 failed（含 A+ 分类、天气/DDG 证据注入、无证据不调用 Codex、复杂 UTF-8 stdin 回归）。
- `cargo test --manifest-path src-tauri/Cargo.toml ai:: -- --nocapture`：17 passed，0 failed。
- `cargo test --manifest-path src-tauri/Cargo.toml voice:: -- --nocapture`：7 passed，0 failed（panic-unwind 用例的 `test unwind` 为预期输出）。
- `cargo check --manifest-path src-tauri/Cargo.toml`、`npx tsc --noEmit`、定向 `rustfmt --check`、`git diff --check` 均通过。
- A+ 真实联网探针：`uapis.cn` 返回兰州 19°C/阴；DuckDuckGo 返回 HTTP 200、10 条摘要；两条证据链均非空。
- 静态契约扫描通过；ASR/KWS/VAD 认可参数未变。
- 当日阻断（已于 2026-07-11 解除）：本机 `~/.codex/config.toml` 曾将数组型 `notify` 放在只接受布尔值的 `[features]` 下，导致 `codex exec` 在模型启动前失败；配置恢复后 Codex 天气问答通过，随后 Provider 切换、CLI/HTTP 取消与 A→B 竞态也均于 2026-07-11 真机验收完成。按需录音提示已由用户确认修复。

### 2026-07-11 多屏选择与位置持久化
- 新增 Rust monitor 模块与 `monitor_list` / `monitor_get_selection` / `monitor_select` 命令，`window:monitor` 保存动态主屏或具体显示器 ID。
- 设置页选择后立即移动；启动恢复所选屏；具体屏缺失时临时回退主屏并保留原选择。
- 自动化覆盖解析、回退、未知 ID、无名称 ID、DPI 宽度和正/负坐标居中（8 passed）。
- 运行时副屏断开回退：`start_runtime_watch` 2 秒轮询，副屏离线即用 `recover_island_to_monitor`（先 `set_size`+`unminimize` 恢复 Windows 最小化到 (-32000,-32000)/160x28 的窗口，再 `set_position` 到主屏）并 emit `monitor://changed(fallback=true)`；副屏恢复时后端不主动重新定位，只复位标志并 emit `fallback=false`；在当时 Windows 真机观察到系统会自动把窗口移回原屏，但该 OS 行为不作为应用稳定保证。设置页监听事件实时更新提示。
- 真机验收当时观察通过：副屏即时移动、重启恢复、动态主屏语义、断开副屏重启回退、运行时断开自动回退主屏，以及该 Windows 环境中的重连自动回原屏；后者是 OS 观察结果，不是后端主动恢复承诺。三态/热键未回归。
- F1.4 当前应用保证是副屏断开时主动回退主屏；恢复时不主动跳回。Windows OS 自动恢复仅为当时真机观察，稳定产品契约转入模块 10 的 DOC-10A-03 / RUN-10C-01。未处理模块 07 动画/性能。

### 2026-07-11 动画打磨完成
- 统一岛级过渡时长/缓动：260ms + cubic-bezier(0.4,0,0.2,1)（需求文档 §6.3）。新增 `src/lib/anim.ts` 集中管理 `ISLAND_DURATION_MS`/`ISLAND_EASE`/`ISLAND_WINDOW_SHRINK_DELAY_MS`，`globals.css` 暴露 `--island-duration`/`--island-ease`；`setState` 收起缩窗延迟 300->280 同步。
- 引入 motion 12，`App.tsx` 用 `AnimatePresence(mode=popLayout)` 包裹 compact/expanded 两处 `CurrentPage`，页面切换横向滑入（Alt+←/->/滚轮方向感知，跳选取最短旋转方向）。
- `NotifyPage` 通知卡片 `AnimatePresence + motion.div`，新通知从顶部滑入+淡入（y:-12->0），`layout` 让既有卡片平滑下移。
- 隐藏->显示（Alt+X）灵动岛淡入：内层容器改 `motion.div`，`islandState=hidden` 时 opacity 0；隐藏态 DOM opacity 已为 0，Rust `window.show()` 后内容从 0 淡入到 1，无闪烁。
- 真机验收：缓动/时长、页面切换滑入、通知卡片滑入、Alt+X 淡入四项均通过；展开/收起过渡未回归。性能优化仍待做。

### 2026-07-11 语音闲置内存优化（ASR 懒加载）
- 真机测得基线：语音未启动 61MB（CPU<1%）、语音唤醒开启 320MB。基线已达标（§4：<60MB 含终端），320MB 是 ASR `OnlineRecognizer` 预建常驻所致（decoder 非 int8 全精度，整个监听会话常驻 ~260MB，但仅唤醒后 8s 录音窗口用到）。
- `voice/mod.rs::run_listen_loop` 移除监听启动时的预建 ASR，改为唤醒命中后 spawn 子线程与「主人我在」TTS 并发懒建（线程内 `CoInitializeEx`，`OnlineRecognizer: Send` 可跨线程 move），TTS 播完 `join` 拿 recognizer 进录音，录完随作用域释放。
- 真机验收：语音开启闲置内存 320->~110MB（base 61 + KWS）；唤醒->「主人我在」->录音->转写链路正常，唤醒延迟无感（ASR 构建藏在 TTS ~1s 后）。手动录音路径（`voice_record_utterance`）本就按需建用完释，未动。

### 2026-07-11 防御性性能优化
- F8.5 毛玻璃降级：新增 `general:blur` 设置（默认开），`App.tsx` 读 + 监听 `settings://changed` 即时生效；关时岛容器去 `backdrop-blur-xl`、`bg-card/70` 改 `bg-card` 实色，降低 GPU 开销（需求文档 §9 对策：透明窗口在某些显卡驱动掉帧）。`GeneralPanel` 总体开关区加「毛玻璃效果」开关。
- 重启托盘项（§9 对策：WebView2 内存随时间增长）：托盘菜单加「重启」项，调用 `AppHandle::request_restart()`（安全退出后重启，比 `restart()` 更可靠触发退出─重启流程）。
- 真机验收：毛玻璃开关即时生效（关后无模糊、改实色背景）、托盘「重启」可用，均通过。

### 2026-07-11 模块 07 收尾
- 模块 07 标记 ✅：设置面板 + NSIS 安装包 + 动画打磨 + 性能优化（语音闲置内存 320→110MB、CPU<1%、基线 61MB、F8.5 毛玻璃降级、重启托盘项）均已完成并真机验收。
- 降至 P2 远期项（不阻塞 07 收尾）：① 终端 tab 主动回收（关停空闲 PTY，降常驻内存，回收前需先确定 dump 输出策略与自动关回收的时机）；② 24h 长跑无泄漏验证（需用户实机长跑，本会话留待真机挂跑后再补验收记录）。

## 提交记录
- chore: 初始化 lite 脚手架与规划文档（项目备忘录 / vault / .gitignore）
- feat(M0): 搭建 Tauri2 + React + TS + Tailwind v4 + shadcn 脚手架
- docs(progress): M0 验证通过（pnpm tauri dev 弹窗成功）
- feat(M1): 灵动岛外壳块1 - 透明窗/托盘/热键/单实例/拖拽/主题切换
- feat(M1): 灵动岛外壳块2 - 三态切换 + 主题跟随系统
- feat(M2): 单页画布切换框架 + 时间页
- feat(M2): 待办页 CRUD + SQLite 持久化
- feat(M2): 日历页（农历/节气/节日 + 月视图导航）
- feat(M3): 天气页 - uapis.cn 拉取 + 离线缓存 + WeatherPage
- feat(M3): 股票页 - 腾讯行情 + 自选股 CRUD + 实时轮询 + StockPage
- feat(M3): 天气多城市 + IP 自动定位
- feat(M3): 股票按名搜索添加（sina suggest typeahead）
- feat(M3): 股票详情 - K线(日/周/月) + 换手等数据，左列表右图布局
- fix(M3): UI 打磨 - 天气城市下拉 / 股票左栏精简 / K图放大+MA图例外置
- fix(M3): 城市去"市"后缀 / 左栏只显股票名 / K线图渲染修复
- feat(M3): 城市/个股拖拽排序 + 紧凑态显示选择
- feat(M4): 终端核心 - xterm + portable-pty 多 tab 桥接
- feat(M4): 快捷命令 + 外部 Windows Terminal 打开
- fix(M4): 终端 tab 状态改模块 store，修复收起/展开重挂载致孤儿 PTY
- fix(M4): 终端无输出 + 多 tab/多城市 UI 堆叠
- fix(M4): 拖拽失效 + 滚轮切换 + 终端无输出诊断
- fix(M4): 回放 PTY 缓存输出 + 终端 tab 拖拽排序/双击改名
- fix(M4): 修复 term_snapshot 借用生命周期编译错误
- feat(M5): 通知后端类型 + SQLite 历史 + token + Tauri 命令
- feat(M5): 本地 HTTP 通知端点 + token 鉴权
- feat(M5): 通知页 + incoming 自动展开跳转
- feat(M5): lucky-notify CLI + Claude/Codex hook 文档
- fix(M5): Cargo default-run=lucky-island，修复多 bin 时 cargo run 选不到
- feat(M5): 通知到达时触发 Windows toast 系统弹窗
- feat(M7): 设置面板 P0 骨架 - 独立窗口(静态声明+close→hide单例) + 总体开关(自启/默认态/toast/主题) + 页面管理(开关/拖拽排序) + settings://changed 即时生效 + 收起动画时序
- fix(M7): lucky-notify 加 no_proxy，修复代理环境 502
- feat(M7): 每页配置（通知来源过滤/天气刷新间隔/股票红涨绿跌）+ 即时生效
- feat(M7): 终端页配置（默认 shell + 字号）+ 即时生效
- feat(M7): 终端快捷命令自定义编辑（F9.8）+ 修复默认终端建两个 tab 竞态
- feat(M7): 快捷命令拖拽排序 + 工具栏多于2个折叠为「更多」下拉
- fix(M7): 终端 compact→expanded 后内容显示不全/滚动异常（ResizeObserver 重 fit）
- fix(M7): 终端 compact→expanded 内容错位（ResizeObserver 改 debounce 200ms）
- feat(M7): 面板内自选股/天气城市 CRUD（F9.8 收尾）
- fix(M7): 天气城市框空输入不弹 + 股票设置显名称并支持拖拽排序
- feat(M7): 天气设置城市列表拖拽排序
- feat(M7): 安装包打包（NSIS + release 优化，5.67MB）
- feat(M7): 统一岛动画时长与缓动曲线（260ms / cubic-bezier(0.4,0,0.2,1)）
- feat(M7): 页面切换横向滑入动画（motion + AnimatePresence popLayout）
- feat(M7): 通知卡片滑入动画
- feat(M7): 隐藏->显示时灵动岛淡入
- docs(M7): 动画打磨完成，更新开发进度
- perf(M7): ASR 唤醒时懒加载，语音闲置内存 320->~110MB
- docs(M7): 记录 ASR 懒加载性能优化与验收
- feat(M7): F8.5 毛玻璃降级开关 + 托盘重启项
