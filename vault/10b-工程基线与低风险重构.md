# 10b-工程基线与低风险重构

> 状态：🚧 进行中（BASE-10B-01/02/03、REF-10B-01/02/03/04/05 已完成；REF-10B-06 Weather B5b 已落地）
> 上游：[10-审计整改.md](./10-审计整改.md)
> 前置：10a 的当前事实和产品契约已明确

## 模块做啥（1 行）

建立可复现验证基线，再以测试保护渐进收敛重复代码和生命周期边界。

## 任务元数据

| 任务 | 状态 | 风险 | 依赖 | 验收方式 |
|---|---|---|---|---|
| BASE-10B-01 | ✅ | 低 | DOC-10A | `pnpm verify` 连续全绿；自动化范围已文档化 |
| BASE-10B-02 | ✅ | 中 | BASE-10B-01 | happy-dom 生命周期 RED-GREEN；统一门禁全绿 |
| BASE-10B-03 | ✅ | 低 | — | 工具链声明 + DLL fixture/真实源检查；安装态另验 |
| REF-10B-01 | ✅ | 中 | BASE-10B-02 | 共享 hook 17/17；业务分批迁移全部收口 |
| REF-10B-02 | ✅ | 低-中 | REF-10B-01 | theme 专项 26/26；全量 verify 全绿；三窗口真机另验 |
| REF-10B-03 | ✅ | 中 | REF-10B-01、registry 契约测试 | 全量前端构建与导航/通知/动画回归 |
| REF-10B-04 | ✅ | 中高/数据 | BASE-10B-01 | 空库/旧库/幂等/失败回滚/防降级 7/7 |
| REF-10B-05 | ✅ | 中/隐私 | BASE-10B-03 | 脱敏+writer+端到端 7/7；启动/退出接入；Release 日志另验 |
| REF-10B-06 | ⬜ | 中 | BASE-10B-02；逐项另定 | 边界校验、并发/状态机定向测试 |

## A. 工程基线

### BASE-10B-01 统一验证入口

> 来源：AUD-BASE-01/02/04/05、AUD-DOC-11

增加并文档化：

- `typecheck`；
- `test:frontend`；
- `test:rust`；
- `check:rust`；
- Rust fmt/clippy；
- 统一 `verify`；
- 三个 HTML/Tauri 窗口入口 smoke build。

必须区分纯逻辑、React 生命周期、Rust 单测、安装态和真机证据；`pnpm test` 不得再被描述为全项目测试。

**2026-07-14 实施与验证记录**：

- RED：`pnpm typecheck` 以 `Command "typecheck" not found` 失败，证明统一命名入口缺失。
- 已在 `package.json` 增加 `typecheck`、`test:frontend`、`build:frontend`、`fmt:rust`、`clippy:rust`、`test:rust`、`check:rust` 与串行 `verify`；`pnpm test` 保留为纯逻辑前端测试兼容别名。
- README 已列出矩阵并明确：Node Vitest 不含 React 生命周期；一次 Vite build 覆盖 `index.html`、`settings.html`、`ai.html`；自动化不替代 GUI、安装态、DLL、Provider 或长跑真机。
- GREEN/最终证据：用户确认采用有界债务清理后，将 16 个 Clippy 项分为 13 个等价机械替换、2 个稳定接口/命名的局部带理由 allow、1 个测试 helper 的 `#[cfg(test)]`；fmt 最终只剩 `lucky-notify.rs` 两处并手工收敛。未降低 `-D warnings`，未运行全仓自动改写。
- 分层动画并发迁移使 `App.layout.test.ts` 的旧 Tailwind 高度字符串断言失效；核实 `App.tsx` 仍保持 380/56 高度和固定 `h-14 shrink-0` 顶栏后，仅把断言更新为当前 Motion inline height 表达，定向视觉回归 12/12 通过。
- 2026-07-14 最新一次连续 `pnpm verify`（Rust 1.92 stable-msvc，`CARGO_TARGET_DIR=.superpowers/target-check`）exit 0：TypeScript 通过；前端 12 files / 118 tests；Vite 产出 `index.html`、`settings.html`、`ai.html`（仅既有主 chunk >500 kB 警告）；全量 Rust fmt 通过；`--all-targets --locked -D warnings` Clippy 通过；Rust lib 73/73；cargo check 通过且无 warning。
- 环境记录：全新 `.omc/base-10b-target` 首次 Clippy 曾长时间停在 `sherpa-onnx-sys` build script；精确终止仅属于该独立 target 的进程树后，复用既有独立缓存 target 完成验证。未触碰默认 `src-tauri/target` 或用户 `tauri dev`。

**完成结论**：统一验证入口、范围声明和严格门禁均已建立并连续全绿。该自动化证据不替代 React 生命周期测试层、GUI、安装态、DLL、外部 Provider 或长跑真机；这些分别属于 BASE-10B-02/03 与 RUN-10C。

### BASE-10B-02 React 生命周期测试层

> 来源：AUD-BASE-01/04、AUD-REF-01

保留 Node 纯逻辑项目，新增可发现 `*.test.tsx` 的 jsdom/happy-dom 或 Browser 测试项目；通过应用自己的 IPC/event 适配层 mock Tauri。首批覆盖 listener 卸载竞态、StrictMode 双挂载、时间跨日/flush 和 AI 跳页。

**2026-07-14 实施与验证记录**：

- 采用最小 happy-dom 层，不引入 Testing Library：Vitest include 扩展到 `*.test.{ts,tsx}`，默认仍为 Node；生命周期测试通过文件级 `@vitest-environment happy-dom` 隔离。`tsconfig.node.json` 同时覆盖 Vite/Vitest 配置。
- 新增 React 19 `act` + `createRoot` 轻量挂载 helper，mock 应用自己的 `onSettingsChanged` 边界，不 mock Tauri 内部实现。
- RED：`useTimeSetting` 三场景中，订阅在卸载前 resolve 的 cleanup 通过；卸载后才 resolve 的 unlisten 为 0；StrictMode 首次挂载订阅也因 cleanup 早于 resolve 而泄漏，结果 2 failed / 1 passed。
- GREEN：只在 `useTimeSetting` 加 disposed/unlisten 交接，并阻止卸载后的初始读取和事件回调更新；专项 3/3 通过。未迁移 App、Stock、Notify、Weather、Terminal 等其他 listener，它们留给 REF-10B-01。
- FIX-10A-03 已按过时契约关闭，因此不恢复 AI 页面跳转测试；已有时间跨日/flush 纯逻辑测试继续保留，生命周期层只验证时间设置 hook 的真实 React 接线。
- 最新连续 `pnpm verify`（独立 Cargo target）exit 0：TypeScript；前端 13 files / 121 tests；三入口 build；全量 Rust fmt；严格 Clippy；Rust lib 74/74；cargo check。Rust 测试增量来自同期模块 11 工作树事实，不归因于本项；build 仅有既有主 chunk >500 kB 警告。

**完成结论**：`*.test.tsx` 可发现，Node 与 DOM 测试环境分离，卸载前/后 resolve 与 StrictMode 双挂载均有真实 React 回归证据。GUI 与全仓 listener 迁移不在本项范围。

### BASE-10B-03 可复现环境与打包前置

> 来源：AUD-BASE-03/05

- 声明 `packageManager`、支持的 Node LTS、Rust stable-msvc；
- 处理旧 `registry.npm.taobao.org` 配置，禁止把证书阻断误报为源码失败；
- 记录 sherpa/onnxruntime DLL 来源、生成与入包检查；
- 文档化联网、Windows 真机、独立 Cargo target 等特殊条件。

**2026-07-15 实施与验证记录**：

- `package.json` 声明 pnpm 10.15.0、Node 22 LTS 范围；新增 `rust-toolchain.toml` 锁定 Rust 1.92.0、x86_64-pc-windows-msvc、rustfmt/clippy。当前实机版本逐项匹配。
- 仓库不新增 `.npmrc`：当前 registry 来自用户级配置且为 `https://registry.npmmirror.com/`；README 增加 `pnpm config get registry` 诊断，明确历史淘宝证书阻断属于环境失败而非源码失败。
- 对照 sherpa-onnx-sys 1.13.4 build script，记录 shared Windows archive 的 k2-fsa GitHub Release 来源、`SHERPA_ONNX_LIB_DIR` / `SHERPA_ONNX_ARCHIVE_DIR` 受控来源，以及 build script 将 lib 目录全部 DLL 复制到 profile 目录的行为。
- 新增跨平台 `check:native-runtime`：从 `tauri.conf.json` 的 bundle resources 读取事实，校验四个必需目标映射、目标不重复、源为非空普通文件。RED 为实现模块缺失；GREEN fixture 4/4 覆盖完整、缺文件、空文件、缺映射。
- 当前默认 `src-tauri/target/release` 的真实检查通过：四个 DLL 映射源均存在且非空。未重新执行 Tauri/NSIS build，本项不宣称安装包内部清单、安装后加载或 PATH 通过；这些保留给 RUN-10C-02/04/06。
- 明确正式打包必须使用默认 target，因为 Tauri 资源路径固定指向 `target/release`；独立 `CARGO_TARGET_DIR` 只用于 check/test/clippy，不直接用于 `pnpm tauri build`。
- 最新连续 `pnpm verify` exit 0：TypeScript；前端 14 files / 127 tests；三入口 build；全量 Rust fmt；严格 Clippy；Rust lib 74/74；cargo check。build 仅有既有主 chunk >500 kB 警告。

**完成结论**：工具链、registry 诊断、原生 DLL 来源及默认 release 打包前置均已可复现和自动检查；联网下载、NSIS 内容与安装态仍按真机矩阵单独验收。

## B. 渐进重构

### REF-10B-01 统一 Tauri event 生命周期（优先）

> 来源：AUD-REF-01

建立 `useTauriEvent`/`useAsyncSubscription`：卸载前完成则正常清理，卸载后才 resolve 则立即 unlisten；handler 用 ref 保持最新；统一 Promise rejection。先测试，再按 `App`、Stock、Notify、Weather、Terminal 分批迁移。

**2026-07-15 首批共享设施记录**：

- 已以 TDD 新增 `useAsyncSubscription` 与 `useTauriEvent`，只建立共享边界，未迁移 App、Stock、Notify、Weather、Terminal 或其他业务 listener。
- 专项 2 files / 17 tests 覆盖卸载前/后 resolve、StrictMode、依赖替换与旧 Promise 晚到、同步/异步订阅失败、disposer 异常、事件最新 handler、事件名变化、enabled 和默认诊断标签。
- 独立审查后将 handler/subscribe ref 改为提交阶段 `useLayoutEffect` 更新，并让每个 effect 捕获自己的错误 options，避免未提交 render 泄漏和旧订阅错误误归新订阅；同一独立审查 Agent 复核确认原问题均解决且无新增高置信度 correctness finding。
- 最新前端验证：`pnpm typecheck` exit 0；`pnpm test:frontend` 16 files / 144 tests；三入口 `pnpm build:frontend` exit 0，仅既有主 chunk 警告。
- 最新连续 `pnpm verify` 使用独立 `.superpowers/target-check` 全部 exit 0：TypeScript；前端 17 files / 146 tests；三入口 build；Rust fmt；严格 Clippy；Rust lib 89/89；cargo check。此前本批之外的 Rust fmt/Clippy 阻断已由对应并行工作收敛，REF-10B-01 未越权修改那些文件。
- **App 批**：已将 settings 适配订阅及 window-state/notify Tauri 事件迁移到共享 hook。App 集成测试 RED 复现通知重订阅和晚 resolve 泄漏，GREEN 4/4；独立审查补强 blur/opacity、旧字符串 payload 的可观察断言后复核无 findings，并确认并行 hover `enable()` hunk 保留。
- App 批最新连续 `pnpm verify` 使用独立 `.superpowers/target-check` 全部 exit 0：TypeScript；前端 19 files / 155 tests；三入口 build；Rust fmt；严格 Clippy；Rust lib 90/90；cargo check。
- **Stock 批**：只将 `stock://tick`、`config://imported` 与 `stock:red_up` settings 订阅迁移到共享 hook，未改变初始 `stock_get`、compact-symbol 或 setting 读取的晚到 Promise 边界，也未修改 Stock 子组件、拖拽、详情或其他页面。
- Stock 集成测试 RED 为 7 项中 3 项失败，准确覆盖卸载后 resolve 未清理、StrictMode disposer 未精确执行和 scoped rejection 诊断缺失；GREEN 专项 Stock + 共享 hook 为 3 files / 24 tests，TypeScript exit 0。
- Stock 批独立只读审查无可执行 finding；确认行为保持、稳定注册、卸载前/后 resolve cleanup、StrictMode 和错误标签均有有效实现与可观察断言。
- Stock 批曾使用独立 `.superpowers/target-check` 完整执行 `pnpm verify` 并全部 exit 0：TypeScript；前端 23 files / 174 tests；三入口 build（仅既有主 chunk 超过 500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 93/93；cargo check。最新复跑中 TypeScript、同数前端测试、三入口 build、严格 Clippy、Rust lib 94/94 和 cargo check 均通过；完整命令仅被并行更新功能在 `src-tauri/src/settings_window.rs:305` 的两条未 rustfmt 展开断言阻断。Stock 专项 3 files / 24 tests 与六路径边界检查仍通过，Stock 批未越权格式化、修改或清理范围外并行工作。
- **NotifyPage 批**：只将 `notify://incoming` 与 `notify:filter_sources` settings 订阅迁移到共享 hook；未改变初始 history/settings Promise、module-scope loader、prepend-before-filter、已读、清理、分页或动画语义。
- NotifyPage 集成测试 RED 为 8 项中 3 项失败，准确覆盖卸载后 resolve 未清理、StrictMode disposer 未精确执行和 scoped rejection 缺失；GREEN 加强后 NotifyPage 9/9，NotifyPage + 共享 hook 为 3 files / 26 tests，TypeScript exit 0。
- NotifyPage 批独立审查先发现初始 `settingGet` 行为未被充分证明；补强调用 key、次数及初始过滤渲染断言后，同一审查 Agent 复核确认 finding 已关闭且无新增可执行 finding。初始 settings 读取晚 resolve 覆盖更新事件的既有竞态仍延后。
- NotifyPage 批使用独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript；前端 24 files / 199 tests；三入口 build（仅既有主 chunk 超过 500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 94/94；cargo check。
- **Weather 批**：只将 `config://imported` 与 `weather:refresh_min` settings 订阅迁入共享 hook；初始城市/settings 读取、refresh timer、当前 `RequestGate`、七日预报/模型、城市操作及请求状态机保持现有工作树语义。
- WeatherPage 集成测试 RED 为 7 项中 6 项失败，准确覆盖城市状态变化后的重复订阅、卸载前/后 disposer 时序、StrictMode 与 scoped rejection 缺口；GREEN WeatherPage 7/7，Weather/Stock/Notify/共享 hook 回归 5 files / 40 tests，TypeScript exit 0。
- Weather 批独立审查发现 latest-closure 测试的导入首项与当前城市相同会产生假阳性；调整为导入 `广州/上海`、继续断言保留当前“上海”后可区分陈旧闭包，复核确认 finding 关闭且无剩余高置信度 correctness finding。
- Weather 批使用独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript；前端 26 files / 211 tests；三入口 build（仅既有主 chunk超过 500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 119 passed / 1 ignored manual probe；cargo check。
- Weather 的初始 settings/城市读取晚到、多次 import 乱序、卸载后业务 Promise、timer/manual/import 重叠及完整按城市 pending/error/generation/在途去重仍归 REF-10B-06；listener disposer 边界通过不代表这些业务竞态已解决。
- **Terminal 批**：将 `terminal:font_size` / `terminal:shortcuts` settings 订阅迁入 `useAsyncSubscription`，并把 xterm `onData` / `onResize` 与 `term://output` / `term://exited` 收敛为 `attachTerminal` 原子资源事务；LIFO cleanup stack 支持部分建立失败回滚、成功 disposer 幂等和单项异常后继续清理。
- Terminal 初始 TDD：bridge 测试 6 项中 4 项 RED，React 生命周期 8 项中 6 项 RED；GREEN 后独立审查继续发现逆序 attach refs、晚 snapshot 写入、pending active fit、旧及卸载中 font read 等异步交错，均逐项补 RED 后修复。最终 Terminal + 共享 hook 专项 4 files / 36 tests，独立审查确认全部 finding 关闭且无新增高置信度 finding。
- Terminal 批保持 `term_write` / `term_resize` / `term_snapshot` 与 output/exited payload 不变；React 卸载只 detach bridge/xterm，模块 store 与 Rust registry 继续保活 PTY，未新增自动 `term_kill`。
- Terminal 批使用独立 `.superpowers/target-check` 的最终 `pnpm verify` 全部 exit 0：TypeScript；前端 28 files / 230 tests；三入口 build（仅既有主 chunk 超过 500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 119 passed / 1 ignored manual probe；cargo check。
- Rust PTY 自然退出 registry 回收、reader join/wait、snapshot/live 重叠、term_create/webview orphan、初始 settings 业务时序和闲置回收产品策略仍延后；未迁移其余设置窗口或 AI listener，未做 GUI、安装态或真机 Tauri 验证。
- **SettingsApp 批**：将 `settings://navigate` 迁入 `useTauriEvent`，保持 payload 白名单解析、侧栏行为和 About 更新检查 phase 规则不变。
- 初始专项通过后，独立只读审查发现 pending `listen()` cleanup 窗口内旧 callback 仍可执行，StrictMode 第一代订阅也可能重复触发 About 更新检查。新增卸载与 StrictMode generation RED 测试，在 `useAsyncSubscription` 向订阅工厂提供每代 effect 独立的 `isActive()`，并由 `useTauriEvent` 在共享层门控业务 handler；未在 SettingsApp 恢复局部 `disposed` 补丁。
- RED 为 3 files / 37 tests 中新增 6 项失败；GREEN 后专项 3 files / 37 tests、listener 回归 6 files / 60 tests。独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript；前端 29 files / 250 tests；三入口 build（仅既有主 chunk >500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 119 passed / 1 ignored manual probe；cargo check。独立复审返回空 findings。
- 未做 GUI、安装态或真机 Tauri 验证；未迁移 GeneralPanel 或 AI listener。下一候选只读设计为 GeneralPanel 的 `monitor://changed` 与 `window://policy-changed` 同组件批次，初始设置/显示器加载 effect 保持范围外。
- **GeneralPanel 批**：将 `monitor://changed` 与 `window://policy-changed` 同批迁入 `useTauriEvent`，分别保护显示器异步刷新与窗口策略原子快照；初始 settings/monitor 加载 effect、开关 mutation、`monitorSelect`、持久化、loading 与 UI 文案不变。
- monitor 事件先采用 payload 并清旧错误，再调用 `monitorList()`；active rejection 写既有 `monitorError` 且不回滚 payload。组件 lifecycle generation 阻止 cleanup 前已启动的晚 resolve/reject 写 state，共享 hook 阻止 cleanup 后 stale callback 启动刷新。policy callback 同步更新 click-through、hover、fullscreen hide 与 support 四字段。
- GeneralPanel 初始 RED 为 13 项中 5 项失败；GREEN 后 GeneralPanel + 共享 hook 专项 3 files / 34 tests、listener 回归 7 files / 73 tests。独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript；前端 30 files / 263 tests；三入口 build（仅既有主 chunk >500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 119 passed / 1 ignored manual probe；cargo check。独立复审返回空 findings。
- 未做副屏热插拔、GUI、安装态或真机 Tauri 验证。剩余 direct listener 静态盘点为 `AiHistoryPanel`、`AiPalette` 与 `VoicePanel`；`settings.ts`/`ai.ts` 是被业务 hook/adapter 调用的低层 helper，`xterm-bridge.ts` 是已完成原子资源事务，不按同类组件 listener 直接迁移。
- **AiHistoryPanel 批**：将 `ai://action-result` 与 `ai://provider-changed` 迁入 `useTauriEvent`；初始 settings/history 加载、provider RPC/回滚、历史搜索/清空、位置重置、Chat API 字段及模型/prompt/request 行为均保持不变。
- action-result 继续调用 `aiHistoryList(500)`；active success 更新历史，active rejection 记录“刷新 AI 对话历史失败”并保留当前列表。component lifecycle generation 阻止 cleanup 前已启动的晚 resolve/reject 写 state 或记录错误，共享 hook 阻止 cleanup 后 stale callback 启动读取。provider callback 仍只接受三个合法值并清除旧切换错误。
- AiHistoryPanel RED 为 18 项中 6 项失败；GREEN 后专项 3 files / 39 tests、扩展 listener 回归 8 files / 91 tests。独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript；前端 31 files / 281 tests；三入口 build（仅既有主 chunk >500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 119 passed / 1 ignored manual probe；cargo check。
- 独立只读复审返回空 findings。未做 GUI、安装态或真机 Tauri 验证；下一候选为 `VoicePanel`，`AiPalette` 因 transcript/send、listening timer 与请求单活状态耦合单独延后。
- **VoicePanel 批**：将 `voice://download-progress` 迁入 `useTauriEvent`，把 `downloadingModel` 依赖重订阅改为单次稳定注册；共享 hook latest handler 始终读取最新 KWS/ASR 下载目标，无额外业务 ref。
- 所有 stage 继续更新 progress；downloading/extracting/未知 stage 保持下载态，error/done 结束下载，done 仅把最新目标对应的 KWS 或 ASR 标为 ready。初始 readiness/settings、download RPC、启停开关、关键词校验/热重载、AiPalette、Rust 与 AI provider/model/prompt 均未修改。
- VoicePanel RED 为 13 项中 8 项失败；GREEN 后专项 3 files / 34 tests、扩展 listener 回归 9 files / 104 tests。独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript；前端 32 files / 294 tests；三入口 build（仅既有主 chunk >500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 119 passed / 1 ignored manual probe；cargo check。独立复审返回空 findings。
- 未做模型下载、GUI、安装态或真机 Tauri 验证；下一候选为 `AiPalette` 的 provider/transcript/listening listener，因 latest send、8 秒 timer 与请求单活状态耦合必须独立设计。
- **AiPalette 批**：将 `ai://provider-changed`、`voice://transcript` 与 `voice://listening` 迁入 `useTauriEvent`；provider 仍只接受三个合法值，transcript 保留可选 payload、trim、空值忽略和单次自动发送，AI 初始化/provider RPC/send/cancel/history/active-request/late-result 语义均未修改。
- transcript 不再通过 passive effect 更新 `sendRef`，而由共享 hook 的提交阶段 latest handler 直接调用最新 `send`；8 秒 listening fallback 改由跨 render 的 `listeningTimerRef` 管理，后端 true/false、timer 重置和卸载清理语义保持。
- AiPalette 初始 RED 为 20 项中 4 项失败，准确复现 provider/transcript 晚 resolve disposer 泄漏、StrictMode 第一代 callback 仍存活及卸载后 transcript 仍发送；GREEN 后 AiPalette + 共享 hook 专项 3 files / 41 tests、扩展 listener 回归 10 files / 120 tests。
- 独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript；前端 33 files / 314 tests；三入口 build（仅既有主 chunk >500 kB 警告）；Rust fmt；严格 Clippy；Rust lib 119 passed / 1 ignored manual probe；cargo check。独立只读复审返回空 findings。
- 未做真实语音、GUI、模型下载、安装态或真机 Tauri 验证。REF-10B-01 只读盘点后确认唯一剩余 React 异步订阅为 `src/components/pages/time/useTimeConfig.ts` 的 `onSettingsChanged`；入口级主题 bootstrap、低层 adapter 与 xterm 资源事务明确不纳入普通 React listener 迁移。
- **useTimeSetting 收口批**：将 `onSettingsChanged` 委托给 `useAsyncSubscription`，保留初始 `settingGet` 的 disposed 保护、`key` 变化重订阅和最新 parser；注册 rejection 进入统一诊断，移除本地 `UnlistenFn`/late-disposer 维护。
- TDD RED 为专项 4 项中 1 项失败，复现 registration rejection 未处理且产生 unhandled rejection；GREEN 后时间设置 + shared hook 专项 3 files / 25 tests、全量前端 33 files / 315 tests，独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript、三入口 build、Rust fmt、严格 Clippy、Rust lib 119 passed / 1 ignored manual probe、cargo check。

### REF-10B-02 共享主题同步

> 来源：AUD-REF-02

提取 `parseThemeMode`、系统主题读取、DOM 应用和 `startThemeSync(): dispose`；React 提供薄 hook。保持 render 前应用主题，防止首帧闪烁；复验岛/设置/AI 三窗口同步。

**2026-07-18 实施与验证记录（并行批次 B1）**：

- 新增 `src/lib/theme.ts`：`parseThemeMode`（非法值返回 null，调用方定回退）、`systemTheme`（带 `matchMedia` 缺失守卫）、`resolveTheme`、`applyTheme`（同步写 `data-theme`）、`startThemeSync(): dispose`（非 React 同步器）、`useTheme`（React 薄 hook）。
- 三处接入：`settings/main.tsx` 与 `ai-palette/main.tsx` 两份逐字重复的 ~25 行模块级 bootstrap 收敛为一行 `startThemeSync({ fallback: "auto" })`（render 前同步应用防闪烁）；`App.tsx` 删本地 `getSystemTheme`/`normalizeThemeMode`/系统 mq effect，改用 `useTheme()` + `parseThemeMode`，`data-theme` 写入 effect 保留。
- 行为保持：解析非法值回退 auto、auto 跟随系统、显式 light/dark 固定、settings://changed 即时切换、render 前同步应用——均与原实现一致，未新增/删除任何主题行为。
- TDD：theme.ts 模块缺失时 RED；GREEN 后 theme 专项 21 tests + useTheme 专项 5 tests。中途修正的均为 happy-dom fake 基建（matchMedia 单例/系统状态），非实现逻辑。
- 独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript、前端 35 files / 341 tests、三入口 build、Rust fmt、严格 Clippy、Rust lib 119 passed / 1 ignored、cargo check；仅有既有主 chunk >500 kB 警告。
- 本批只收敛三处主题重复，未改主题行为、未拆 App；岛/设置/AI 三窗口真机视觉同步未在本批验收（归 RUN-10C）。

### REF-10B-03 轻量拆分 App

> 来源：AUD-REF-03
> 依赖：REF-10B-01、页面 registry 契约测试

只提取页面 registry、`useIslandSettings`、`useIslandEvents` 和导航；保留根窗口状态。Terminal 等重资源页面是否保活单独评估，不让所有隐藏页面永久运行定时器/监听器。

**2026-07-18 实施与验证记录**：

- 拆分边界与保活策略由用户确认：一次拆四块 + 沿用现状单页画布（仅当前页挂载；Terminal 内部已用 term-store 保活 PTY）。
- 新增 `src/pages/registry.ts`（`ALL_PAGES`/`PAGE_BY_ID`/`PageMeta`）、`src/pages/useIslandSettings.ts`（settings KV 初始化 + settings://changed 订阅）、`src/pages/useIslandEvents.ts`（windowPolicyGet 启动读取 + `window://state-changed`/`notify://incoming` 订阅）、`src/pages/useIslandNavigation.ts`（pageIndex/direction/setPage + 滚轮 + Alt 快捷键 + Escape）。
- `App.tsx` 收敛为岛容器/动画/顶栏/页面渲染；保留 transitionController/hoverController/theme/update-store/window-policy 顶层状态，不迁入 hook。行数 568→373（-34%）。
- 契约保护：既有 `App.events.test.tsx`（4 项：notify 注册稳定+latest pages、settings/window-state 双形态、卸载后 disposer、StrictMode 双跑清理）与 `App.layout.test.ts`（顶栏布局）保持绿；新增 `App.structure.test.ts` 4 项 RED→GREEN 锁定拆分结构与行数收敛。
- 行为保持：单页画布、紧凑/展开动画、settings 启动+变更应用、window-policy 快照与字符串双形态、notify 跳页、Alt 快捷键、滚轮切页、Escape 收起均与拆分前一致；未引入 keepAlive，Terminal 等仍仅当前页挂载。
- 独立 `.superpowers/target-check` 的 `pnpm verify`（含 `SHERPA_ONNX_LIB_DIR` 绝对路径与 `CARGO_HTTP_CHECK_REVOKE=false`）exit 0：TypeScript、前端 37 files / 361 tests、三入口 build（仅既有主 chunk >500 kB 警告）、Rust fmt、严格 Clippy、Rust lib 133 passed / 1 ignored、cargo check。
- 未做 GUI/真机窗口交互验证；导航 hook 把滚轮 effect 与快捷键 effect 一并迁出，未单独拆「岛容器」组件，避免一次引入多层边界。

### REF-10B-04 SQLite 版本化 migration

> 来源：AUD-REF-04

采用 `PRAGMA user_version` 或 `schema_migrations`；当前 schema 为基线；每版事务执行。测试空库、旧库、重复执行、失败回滚。物理 SQL 目录次要；不顺便引入 ORM/连接池。因涉及用户数据，实施时独立批次与备份策略。

**2026-07-18 实施与验证记录（并行批次 B3）**：

- 把 `Db::init` 的「基线 `CREATE TABLE IF NOT EXISTS` 全 schema + 特例 `migrate_notifications`」收敛为 `PRAGMA user_version` 版本化迁移，位于 `storage/mod.rs`，未引入 ORM/连接池。
- `SCHEMA_VERSION = 2`：v1 基线六表（新建库直接含 notifications.priority 最终形态），v2 `migrate_notifications` 为旧库补 priority 列（幂等）。`MIGRATION_STEPS` 升序，`debug_assert` 最后一版等于 `SCHEMA_VERSION` 防漂移。
- 每版在自身事务内执行结构变更（`unchecked_transaction` 以 `&Connection` 表达，单连接无嵌套），提交后事务外推进 `user_version`（SQLite 不允许事务内写）。失败回滚该版、`user_version` 不前进、可断点续传。
- 防降级：库的 `user_version` 高于 `SCHEMA_VERSION` 时拒绝打开并报版本号，避免旧代码静默读写新版结构。
- TDD：`run_migrations`/`SCHEMA_VERSION` 缺失时 RED（E0432）；GREEN 后 migration_tests 7/7：空库建全 schema+version、旧库升级+数据保留、二次幂等、v1 部分迁移只补后续、已最新不动数据、失败回滚不前进+续传、新版库拒绝。
- 独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript、前端 35 files / 341 tests、三入口 build、Rust fmt、严格 Clippy、Rust lib 126 passed / 1 ignored、cargo check。
- 本批只改 `storage/mod.rs` 迁移装配，未动业务读写、未改 schema 内容；真实用户库迁移属数据敏感操作，未在真机/既有数据上执行（备份策略与真机验证归 RUN-10C）。

### REF-10B-05 持久、脱敏、轮转日志

> 来源：AUD-REF-05

覆盖启动/退出、Provider 开始取消超时失败、CLI 退出、HTTP 绑定、PTY、显示器回退、语音模型失败。禁止 API key、token、完整 prompt/对话。Release 下可定位日志，设置大小/天数轮转；UI 错误仍需可理解。

**2026-07-18 实施与验证记录（并行批次 B4）**：

- 用户决策：技术方案 `tracing + tracing-appender`；本批范围「设施 + 启动/退出接入」，其余 6 类日志点后续小批逐个接入。
- 新增 `src-tauri/src/logging.rs`：
  - `redact` 脱敏：`Authorization: Bearer`、`token=/api_key=/apikey=/api-key=/access_token=/secret=/password=` 命名参数（参数名边界防 `mytoken=` 误伤）、≥24 位高熵裸 token（hex/JWT/UUID 链）；短普通词不误伤。
  - `RedactingWriter`/`RedactingMakeWriter`：包装 tracing fmt 的 writer，每条事件写入前统一 `redact`，保证落盘不含密钥。
  - `init_logging`：`app_log_dir` 下 `lucky-island.log` 按天轮转（tracing-appender `rolling::daily` + 非阻塞 writer），`EnvFilter` 默认 `info`、可被 `RUST_LOG` 覆盖，Release 下可定位（不依赖 debug_assertions）。返回的 `WorkerGuard` 经 `LoggingGuard`（`tauri::Resource`）持有到进程退出 resource 清理时 drop flush。
- 接入：`lib.rs` setup 最早期初始化（失败回退 stderr），记「启动（带版本）」与「退出开始清理」两条；其余 `eprintln!` 路径（Provider/CLI/HTTP/PTY/显示器/语音）未改，留后续小批。
- 依赖：`Cargo.toml` 新增 `tracing`/`tracing-subscriber`(fmt,env-filter,time)/`tracing-appender`；`Cargo.lock` 纯新增 tracing 传递依赖，无既有包版本变化。
- TDD：模块缺失 RED；GREEN 后 logging 7/7：bearer、命名参数、高熵裸 token、正常文本不误伤、参数名边界、writer 写入前脱敏、subscriber 端到端脱敏。
- 独立 `.superpowers/target-check` 的 `pnpm verify` 全部 exit 0：TypeScript、前端 35 files / 341 tests、三入口 build、Rust fmt、严格 Clippy、Rust lib 133 passed / 1 ignored、cargo check。
- **环境约束**：新增 tracing 触发 sherpa-onnx 重链，暴露既有脆弱性——`sherpa-onnx-sys` build script 用**相对** `cargo:rustc-link-search`，`pnpm verify` 不带 `SHERPA_ONNX_LIB_DIR` 时 link 进程 cwd 解析失败（LNK1181）。本批验证通过设 `SHERPA_ONNX_LIB_DIR` 为**绝对路径**（指向 `.superpowers/target-check/sherpa-onnx-prebuilt/.../lib`）+ `CARGO_HTTP_CHECK_REVOKE=false`（离线 SSL 撤销检查）完成。这是环境/toolchain 约束，非产品代码问题；后续 verify 同样需带这两个 env。
- Release 安装包内日志轮转实际行为、密钥扫描真机验证未执行（归 RUN-10C-04/06）。

### REF-10B-06 后续渐进项

> 来源：AUD-REF-06

- IPC 原始响应按 unknown 校验，先 AI 历史/通知/天气/股票；
- ~~Weather 按城市管理 pending/error/generation 与请求去重~~（✅ 批次 B5b）；
- AiPalette 先用 reducer/state machine 测试取消/晚到响应，再拆 Provider/语音/UI；
- 设置字段分离 draft/persisted，按字段 debounce/blur，并显示保存错误。

这些是 P2，不一次性全量迁移，也不为了类型美观引入大型代码生成体系。

**2026-07-18 实施与验证记录（批次 B5b / Weather 按城市请求状态机）**：

- 删除原全局 `RequestGate`（active/compact 两套），改为按城市独立的 `CityFetchEntry`（token/generation/pending/inflight/error/candidates），位于 `src/components/pages/weather/model.ts`，导出 `beginCityFetch`/`cityFetchOutcome`/`emptyCityFetchEntry`/`failCityFetch`/`resolveCityFetch` 纯函数供组件与测试复用。
- 组件侧 `WeatherPage.tsx` 用同步 ref（`fetchStatesRef`）作 token 真源、React state 仅作渲染派生：begin 同步读 ref 判去重，响应回抵后先凭 token 校验再写 `fetchStates` 与 `cache`，杜绝「ref/state 双 begin 生成两枚 token」与「cache 写在 token 校验前」两类回归。`inflightRef` 已删除，token 只有一份真源。
- 关键不变量由测试保护：同城市 inflight 复用 token 且 generation 不递增；串城市/旧 token/已丢弃 entry 的响应一律 ignored；成功/失败/ambiguous 三路结果互斥清空；删除城市后该城市任何晚到响应不得落缓存。
- 渲染派生保持旧行为：active 城市驱动 err/candidates/spinner；compact 城市独立 inflight 驱动「天气…」首载文案；err 仅在无缓存时整页展示；有缓存的失败仅清 inflight、不覆盖缓存。
- 初始挂载 effect 收敛为 once（`didInitRef` 防 StrictMode 双跑 + 仅依赖 ref/stable callback），避免每次切 active 重建 effect 触发重复请求。
- 测试：状态机纯逻辑 `city-fetch.test.ts` 13 项（首 begin/dedupe/跨城市独立/晚到忽略/删除忽略/三路结果/generation 单调）；组件层 `WeatherPage.test.tsx` 新增 3 项（inflight 归位、删除后晚到不落缓存、refetch 去重），加上既有订阅/timer/StrictMode 用例共 10 项；Weather 域合计 23 项。
- 验证：定向 `pnpm vitest run` Weather 2 文件 23/23 通过；完整 `pnpm verify`（独立 `.superpowers/target-check`，含 `SHERPA_ONNX_LIB_DIR` 绝对路径与 `CARGO_HTTP_CHECK_REVOKE=false`）exit 0：TypeScript、前端 36 files / 357 tests、三入口 build（仅既有主 chunk >500 kB 警告）、Rust fmt、严格 Clippy、Rust lib 133 passed / 1 ignored、cargo check。
- 未做 GUI/真机天气界面验证；删除紧凑城市后空 compactCity 的极端降级路径沿用旧行为未单独立项。

**2026-07-18 实施与验证记录（批次 B5c / IPC unknown 校验）**：

- 不引入 zod/valibot 等校验库；新增轻量 `src/lib/ipc-guard.ts`（`Guard<T>`/`isObj/isStr/isNum/isBool/isArr/isNullable/isOneOf/hasStrFields/hasNumFields/assertIpc`）+ `src/lib/ipc-schemas.ts`（按域集中导出守卫）。
- 覆盖 4 个目标域：
  - **AI**：`ai_chat`/`ai_cancel`/`ai_history_list` 三个 invoke 全部 unknown→schema 校验；`ai://action-result` 事件 payload 同步校验，非法 payload 走 `console.error` 不再静默注入。
  - **通知**：`notify_list` 校验 `NotificationItem[]`，含 `priority` 三态枚举与 `action_type: "open_terminal"` 字面量。
  - **天气**：`weather_get`/`weather_cities_list`/`weather_locate`/`weather_location_search` 全部接入；嵌套 `WeatherAlert`/`WeatherDay`/`WeatherLocation`/`WeatherSourceInfo` 全字段校验。
  - **股票**：`stock_get`/`stock_search`/`stock_kline` + `stock://tick` 事件 payload 同步校验；`Quote` 22 个字段全数值/字符串强约束。
- 调用点统一改成 `invoke<unknown>` + `assertIpc(label, raw, guard)`，校验失败抛 `[ipc] <label> 响应不符合契约` 进入既有 catch，不引入新错误码体系。类型上调用点依旧拿到本地业务接口（`Quote`/`NotificationItem`/`WeatherBundle` 等）。
- 测试：新增 `src/lib/__tests__/ipc-schemas.test.ts` 16 项，覆盖每类守卫的合法接受 + 至少 2 类非法拒绝（缺字段/错类型/枚举越界/nullable 边界）+ `assertIpc` 抛错标签。
- 独立 `.superpowers/target-check` 的 `pnpm verify` exit 0：TypeScript、前端 38 files / 377 tests、三入口 build（仅既有主 chunk >500 kB 警告）、Rust fmt、严格 Clippy、Rust lib 133 passed / 1 ignored、cargo check。
- 未做 GUI/真机验证；`ai_history_list` 后端契约以「元组前 2 列即 role/content」表述（后端尚有未用第 3 列，按现状宽进）。`settings_list`/`window_policy_*` 等 lib 内 invoke 未纳入本批，留待下一 P2 批。

## 依赖顺序

```text
BASE-10B-01/02
  └─ REF-10B-01 Tauri event hook
       ├─ REF-10B-02 主题共享
       └─ REF-10B-03 App 拆分

REF-10B-04 migration（独立数据批次）
REF-10B-05 logging（可独立，宜早于高风险探针）
REF-10B-06（P2 渐进）
```

## 完成门禁

- [ ] 每项先有行为保护测试或明确不变量。
- [ ] 每个批次只改变一个主要边界，失败可回滚。
- [ ] 无新增第二配置真源或无证据依赖。
- [ ] 自动化命令、环境、日期和结果均落盘。
- [ ] 涉及 Windows/Tauri 的行为另有真机记录。
