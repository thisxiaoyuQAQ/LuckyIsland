# 10-审计整改

> 状态：✅ 整改全部落地（DOC-10A / FIX-10A / BASE-10B / REF-10B / RUN-10C 全部完成；RISK-10C / PROD-10C 为开放项，待复现证据或产品决策）
> 审计来源：[2026-07-13 项目与文档同步审计](../docs/审计/2026-07-13-项目与文档同步审计.md)
> 当前入口：[CURRENT.md](./CURRENT.md)

## 模块做啥（1 行）

将六路审计发现转成可追溯、可验证、按风险分层的整改任务，并同步项目事实源。

## 非目标

- 不推倒重写 React/Tauri/SQLite 主架构。
- 不为匹配旧规划引入 Zustand、TanStack Query 或 TOML 双配置源。
- 不把历史 plan 未勾选项机械转成 TODO。
- 不重开已关闭的 BUG-20260710-01～05 或 08a。
- 不在缺乏复现/产品决策时直接实施高风险候选。

## 状态

⬜ 待处理 | 🚧 进行中 | ✅ 已完成 | 🚫 阻塞 | 🔎 待复现 | 🧭 待决策

## 任务总表

| 任务 ID | 范围 | 状态 | 风险 | 依赖 | 执行单 |
|---|---|---|---|---|---|
| DOC-10A | 文档事实源同步 | ✅ | 低 | — | [10a](./10a-文档同步与确定性修复.md)；DOC-10A-01～03 ✅ |
| FIX-10A | 三个确定性行为缺口 | ✅ | 中 | DOC-10A（已完成） | [10a](./10a-文档同步与确定性修复.md)；01/02 已修复，03 过时前提关闭 |
| BASE-10B | 工程环境与统一验证基线 | ✅ | 低-中 | DOC-10A | [10b](./10b-工程基线与低风险重构.md)；01/02/03 ✅ |
| REF-10B | 渐进低风险重构 | ✅ | 中 | BASE-10B | [10b](./10b-工程基线与低风险重构.md)；01～06 ✅ |
| RISK-10C | 高风险候选探针/设计 | 🔎 | 高 | BASE-10B | [10c](./10c-高风险候选与产品验证.md) |
| PROD-10C | 产品范围决策 | 🧭 | 产品 | DOC-10A | [10c](./10c-高风险候选与产品验证.md) |
| RUN-10C | 真机与安装态矩阵 | ✅ | 环境 | 对应实现/用户真机 | [10c](./10c-高风险候选与产品验证.md)；2026-07-19 全部通过 |

## 审计映射

| 审计类别 | 审计 ID | 任务 |
|---|---|---|
| 文档事实 | AUD-DOC-01～11 | DOC-10A-01～03、BASE-10B-01 |
| 确定性缺陷 | AUD-FIX-01～03 | FIX-10A-01～03 |
| 工程基线 | AUD-BASE-01～05 | BASE-10B-01～03 |
| 渐进重构 | AUD-REF-01～07 | REF-10B-01～06、RISK-10C-03 |
| 高风险候选 | AUD-RISK-01～06 | RISK-10C-01～06 |
| 产品范围 | AUD-PROD-01～05 | PROD-10C-01～05 |
| 真机验证 | AUD-RUNTIME-01～06 | RUN-10C-01～06 |

## 执行批次

```text
批次 0：DOC-10A 文档事实源同步
  └─ 批次 1：FIX-10A 三个确定性缺陷
       └─ 批次 2：BASE-10B 工程基线
            └─ 批次 3：REF-10B 渐进重构
                 └─ 批次 4：RISK/PROD/RUN 探针、决策、真机
                      └─ 批次 5：全量复核与关闭模块 10
```

FIX 与 BASE 可在文档契约明确后按文件冲突情况交错，但同一提交/验收批次不要混合 SQLite migration、PTY、语音或 CSP 等高风险改动。

## 进度记录

### 2026-07-13 DOC-10A-01 完成

- 核心文档与 vault/02、06、08 已按当前实现收敛；明确所有早期未采用方案和合法历史例外。
- 已核验 manifest、插件、存储 schema、通知 HTTP、三个 AI Provider、各 Provider 联网边界、AI 非流式完整回复、前端 pending/真正取消、语音流式 ASR、多屏断开回退与双动作热键。
- 未修改产品源码，未把历史测试数字当作新证据；DOC-10A 总任务保持进行中，顺序进入 DOC-10A-02。

### 2026-07-13 DOC-10A-02 完成

- 4 份已实施 spec 已补最终状态、当前验收链接和偏离摘要；5 份 plan 已标为历史实施脚本，189 个未勾选框保留历史语境且不再构成当前 TODO。
- 时间页测试快照已区分历史 Vitest 3/42 passed 与当前锁文件 Vitest 4.1.10、既有验收 45 passed，并明确纯逻辑/node 范围；素材仅记录为用户提供，未虚构仓库内许可证证明。
- 旧“下一 Session”“待真机”只存在于带历史声明的 plan；本轮仅改文档，`git diff --check` 通过，未产生新产品测试证据。该完成时点的下一项为 DOC-10A-03；该契约任务现已完成。

### 2026-07-13 DOC-10A-03 完成

- 用户确认副屏恢复后应由应用主动移回已保存副屏；当前实现只有断开回退，新增行为与回归归入模块 11，Windows 自动移回观察不作为实现证据。
- Hidden 与鼠标穿透已分离：Hidden 使用 `window.hide()`；模块 11 后续实现默认关闭、可持久化、可选全局热键恢复的整窗手动穿透，不做透明区域选择性命中。
- README、PRD、技术栈、开发进度、vault/02、10a/10c/CURRENT 与模块 11 spec/plan/vault 已按同一契约同步；未修改产品源码，DOC-10A 完成，下一项为 FIX-10A-01。

### 2026-07-14 FIX-10A-01 完成

- 先建立 23:59 → 00:00 RED：Vitest 进入项目后因共享 `localDayStore` 缺失而失败；随后实现模块级单 timer、visible 补偿与最后订阅者清理。
- 木鱼按共享日期 rollover 并持久化新日状态；心情按新日期重载当天记录/streak 并隔离旧异步结果；未夹带 FIX-10A-02 卸载 flush。
- store 3/3、日期+store 17/17、完整前端 48/48、TypeScript 与三入口 build 均通过；真实午夜 Windows 长时间观察未额外执行。FIX-10A 当前进入 FIX-10A-02。

### 2026-07-14 FIX-10A-02 完成并暂停模块 10

- 纯 writer 测试保护 500ms 到期前 `flush()` 只写 latest；原始 cleanup 缺陷没有保留历史 RED 命令输出，未补造。留存 RED 分别证明：定时器触发的 latest 写失败后未被重试（expected 2 writes, got 1）、旧失败值会在更新值成功后残留，以及并发 `flush()` 会重复重试相同 latest 失败值（actual 4 writes, expected 3）。
- 使用组件实例私有的 `DebouncedWriter`：正常敲击保留 500ms debounce，卸载和跨日调用 `flush()`；写入链及并发 `flush()` 严格串行，定时写失败可重试，更新值成功后清除更旧失败状态。加载前禁用并保护点击；读取 rejection 或字段畸形的非空存量显示安全零值但保持禁用，避免覆盖未知历史；设置读取跨过午夜时使用读取完成后的 `currentLocalDay()` 归日，点击时也先按实时日期 rollover。
- writer 8/8、woodenFishState 6/6、writer + woodenFishState + localDayStore + date 聚焦测试 31/31、完整 Vitest 106/106、TypeScript 与三入口 Vite build 均通过；全量 diff check 通过。构建仅有既有主 chunk >500 kB 警告；GUI 快速切页未额外真机验收。
- 按 2026-07-14 用户确认的续接顺序，模块 10 保留 FIX-10A-03、10b/10c 并暂停；模块 11 Task 2 已完成，当前唯一入口为 Phase 1 / Task 3。插件市场及语音/问答插件化仍只保留规划。

### 2026-07-14 FIX-10A-03 核实关闭

- 全仓没有生产端 `switch_page` 或 `island://switch-page`；`ai/router.rs` 只接受 `reply`/`add_todo`，未知动作明确失败，`ai/prompt.rs` 也明确禁止 AI 操控岛窗口。
- 原 AUD-FIX-03 基于 DOC-10A 同步前的旧文档设想，当前不存在“动作成功但页面未切换”的可触发路径。用户确认按过时审计项关闭，不重新引入 AI 页面控制能力。
- 本项仅同步审计状态与证据，未修改产品源码、未虚构运行测试；FIX-10A 至此完成，模块 10 进入 BASE-10B。

### 2026-07-14 BASE-10B-01 统一入口与严格门禁完成

- `package.json` 与 README 已增加并解释 TypeScript、纯逻辑前端测试、三入口 build、Rust fmt/clippy/test/check 和串行 `verify`；`pnpm test` 明确只是 `test:frontend` 兼容别名。
- 用户确认有界清理后，16 个 Clippy 债务按等价机械替换、稳定契约局部 allow 和测试 helper 收窄处理；Rust fmt 最终只手工收敛 `lucky-notify.rs` 两处，没有降低门禁或运行全仓自动改写。
- 模块 11 分层动画令旧布局测试绑定的 Tailwind 高度字符串过时；核实 380/56 高度与固定 `h-14 shrink-0` 顶栏不变量仍成立后，仅同步测试到当前 Motion 表达。
- 最新连续 `pnpm verify` exit 0：TypeScript、前端 118/118、三 HTML 入口 build、全量 Rust fmt、严格 Clippy、Rust lib 73/73、cargo check 均通过；build 仅有既有主 chunk >500 kB 警告。自动化不替代 GUI、安装态、DLL、Provider 或长跑真机。

### 2026-07-14 BASE-10B-02 React 生命周期测试层完成

- 新增 happy-dom 文件级环境和 React 19 轻量挂载 helper；现有纯逻辑测试继续使用 Node，Vitest 可发现 `*.test.tsx`，TypeScript 配置覆盖 Vite/Vitest。
- `useTimeSetting` RED 为 2 failed / 1 passed：卸载后订阅才 resolve 时 unlisten 为 0，StrictMode 首次挂载同样泄漏；卸载前 resolve 的正常 cleanup 通过。
- 最小修复只加入 disposed/unlisten 交接并阻止卸载后更新，专项 3/3；没有迁移其他 listener，也没有恢复已关闭的 AI 页面跳转契约。
- 最新连续 `pnpm verify` exit 0：TypeScript、前端 121/121、三入口 build、Rust fmt、严格 Clippy、Rust 74/74、cargo check。build 仅有既有主 chunk >500 kB 警告；GUI 与真机未执行。

### 2026-07-15 BASE-10B-03 可复现环境与打包前置完成

- 声明 Node 22 LTS、pnpm 10.15.0 与 Rust 1.92 stable-msvc；仓库不新增 registry 真源，README 明确当前配置诊断与历史证书错误边界。
- 记录 sherpa 1.13.4 shared DLL 的上游下载/本地归档环境变量、build script 复制行为，以及正式 Tauri 打包必须使用默认 `src-tauri/target/release` 的约束。
- 新增 `check:native-runtime`，fixture 4/4 覆盖完整、缺文件、空文件、缺映射；当前默认 release 的四个非空 DLL 映射源真实检查通过。
- 未重新打 NSIS，不宣称安装包内部或安装后 DLL 加载通过。最新连续 `pnpm verify`：前端 127/127、Rust 74/74，其余严格门禁全绿。

## 完成定义

- [ ] 所有审计项均处于：已完成 / 不做有理由 / 历史完成覆盖 / 待用户决策 / 观察项之一。
- [ ] 三个确定性缺陷有失败测试、最小修复和回归证据。
- [ ] 统一验证入口可在声明的环境中执行，自动化与真机证据分开记录。
- [ ] 高风险候选未被无证据直接实施；派生任务具有新失败场景。
- [ ] `项目备忘录.md`、`docs/开发进度.md`、本文件与 `CURRENT.md` 状态一致。
- [ ] 审计报告的遗漏复核全部通过。

## 状态回写顺序

1. 更新具体 `10a/10b/10c` 任务和证据；
2. 更新本总控表；
3. 更新 `docs/开发进度.md`；
4. 将 `CURRENT.md` 改为唯一下一动作；
5. 仅在真正改变需求/架构时更新 PRD/技术栈，不复制任务过程。
